# 审计发现表

| 编号 | 发现                                                                            | 类型     | 影响                                       | 关键证据                                                                                     | 建议                                           |
| ---- | ------------------------------------------------------------------------------- | -------- | ------------------------------------------ | -------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| F1   | 三层分工方向是对的：builtin 控制面、Mem0 作用域治理、Graphiti 关系时序          | 优点     | 决定了这套方案值得继续推进                 | `analysis/.../README.md:4`                                                                   | 保留为 W5 核心架构前提                         |
| F2   | builtin local 不是弱兜底，而是有混合检索、MMR、时序衰减的真实能力层             | 优点     | 让 local 可以承担质量基线和精确保护        | `src/memory/backend-config.ts:72`, `src/memory/hybrid.ts:51`                                 | 继续保留 builtin 作为默认 local                |
| F3   | 前期过早追求三路融合，晚了才补 upstream 契约与作用域字段                        | 设计缺陷 | 造成早期排障反复、本地试错偏多             | `analysis/.../README.md:30`, `extensions/.../mem0-client.ts:424`                             | W5 前把契约测试正式化                          |
| F4   | Graphiti 长期被当成 generic `/search` 后端，图能力利用不足                      | 设计缺陷 | 有增益，但上限没吃满                       | `analysis/.../README.md:27`, `extensions/.../graphiti-client.ts:83`                          | W5 早期推进图原生能力接入                      |
| F5   | bridge 曾经把远端错误吞成空结果，导致“看起来没提升、实际是请求坏了”             | 实现缺陷 | 直接拖慢排障，误导判断                     | `extensions/.../tools/memory-search-tool.ts:1727`                                            | 保持 typed error + fallback reason，不允许再吞 |
| F6   | Mem0 作用域字段最初没补齐，说明对上游契约理解不足                               | 实现缺陷 | 会直接导致 500 或空结果                    | `extensions/.../mem0-client.ts:20`, `extensions/.../mem0-client.ts:424`                      | 增加契约 smoke 和字段缺失测试                  |
| F7   | 当前 local fallback 依赖远端嵌入服务，命名容易让人误以为是纯离线层              | 设计缺陷 | 容易造成错误预期和错误运维决策             | `src/agents/tools/memory-tool.ts:55`                                                         | 在文档与配置口径上明确边界                     |
| F8   | self-host memory stack 依赖 repo 外运行目录、pinned refs 和 patch，运维负担较重 | 风险点   | 扩流阶段会放大环境差异与维护成本           | `docs/operations/memory-stack-selfhost.md:8`, `docs/operations/memory-stack-selfhost.md:24`  | W5 增加 stack fingerprint 与兼容 smoke         |
| F9   | 单主远端 + 失败再补另一条的融合策略明显优于无条件双远端并行                     | 优点     | 真正把 p95 拉回门槛内                      | `extensions/.../memory-search-tool.ts:2502`, `analysis/.../day33/.../w4-gate4-delta.json:25` | 作为 W5 默认策略保留                           |
| F10  | 将 Graphiti `max_facts` 收到 1 后，Gate-4 最终过线                              | 优点     | 证明尾延迟主要来自 Graphiti 搜索尾部工作量 | `extensions/.../graphiti-client.ts:90`, `analysis/.../day33/.../w4-gate4-delta.json:25`      | 继续保留，同时规划更优雅的限组优化             |
| F11  | 社区已报告多 `group_id` 全文检索风险，这会影响下一步性能优化方案                | 风险点   | 不谨慎会伤质量                             | `https://github.com/getzep/graphiti/issues/1249`                                             | W5 若做 `group_ids`，先单组或先修缺陷          |
| F12  | bridge 核心逻辑过度集中在单文件，长期演进风险高                                 | 实现缺陷 | 每次改动都更容易互相影响                   | `extensions/memory-mem0-graphiti-bridge/src/tools/memory-search-tool.ts`                     | W5 前半程做模块拆分                            |
